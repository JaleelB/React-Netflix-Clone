import React from 'react'

const Saved = () => {
    return (
        <div style={{height: '100vh', width: '100vw', background: 'teal'}}>
            <h1>Saved</h1>
        </div>
    )
}

export default Saved
